# partial_test.meta.zbr — auto-generated metadata methods (simulates codegen output)
class PartialTest
    shared
        def describe(name as str, value as int) as str
            return name + ": " + value.toString()
