class Main
    shared
        def main
            var args = sys.args()
            print args.count()
            sys.errln("hello stderr")
            if args.count() > 1
                print args.at(1)
